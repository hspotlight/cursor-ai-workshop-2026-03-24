{
  "name": "velocty",
  "version": "1.0.0",
  "releaseDate": "2026-03-19",
  "description": "Velocty — a fast, simple todo app for teaching full-stack development",
  "scripts": {
    "serve": "python3 -m http.server 3000 --directory public",
    "test": "jest",
    "test:watch": "jest --watch",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
